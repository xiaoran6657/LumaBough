# 组合根：统一 sandbox 在启动代码解析 --rhi/--renderer，经 factory 取得后端。
# 旧 concrete samples（samples/sandbox、samples/sandbox_d3d12）已于 2026-09-16 退役
# （LEGACY-SAMPLES-RETIRE），需要时可从 git 历史找回；它们不再进入构建。
add_subdirectory(rhi_sandbox)
